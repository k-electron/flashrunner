// DISPOSABLE SCAFFOLD CONTENT — deleted by feature 001-deck-runs.
// A pure function, so the plain unit-test path is proven without a DOM.
export function greeting(name: string): string {
  return `Hello, ${name}!`;
}
